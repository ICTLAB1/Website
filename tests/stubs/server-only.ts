/**
 * Test stub for the `server-only` package.
 *
 * The real module throws when it is loaded from a client bundle, which is a
 * build-time guarantee provided by Next.js. Unit tests run in plain Node, where
 * that guard is neither meaningful nor satisfiable, so it is replaced with a
 * no-op. Nothing in the application imports this file.
 */
export {};
